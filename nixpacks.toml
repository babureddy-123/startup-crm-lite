# Nixpacks configuration to build and run the backend on Railway
[phases.install]
cmds = ["if [ -d \"backend\" ]; then cd backend && npm install; else npm install; fi"]

[phases.build]
cmds = []

[start]
cmd = "if [ -d \"backend\" ]; then cd backend && npm start; else npm start; fi"
